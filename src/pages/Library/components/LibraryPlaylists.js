import { useGetSpotifyDataQuery } from "../../../services/spotify";

const LibraryPlaylists = () => {
  // const { data } = useGetSpotifyDataQuery("me/albums");
  // console.log(data);
  return <div>LibraryPlaylists</div>;
};

export default LibraryPlaylists;
